# Helpdesk Platform Example

This example shows how the kit can document a non-game software product.

Assume the root framework files from this repo are already present in the target repository. The files in `documentation/` below are project-specific examples that sit on top of the framework:

- a system definition for ticket routing
- a workflow definition for VIP escalation
- a principles document for support operations
- a lightweight architecture reference
